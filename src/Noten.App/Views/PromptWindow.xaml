<Window x:Class="Noten.App.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt" Height="160" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock" Text="Value"/>
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Margin="0,8,0,0" MinWidth="360"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="86" Margin="0,8,8,0" IsDefault="True" Click="Ok_Click" Content="OK"/>
            <Button Width="86" Margin="0,8,0,0" IsCancel="True" Content="Cancel"/>
        </StackPanel>
    </Grid>
</Window>

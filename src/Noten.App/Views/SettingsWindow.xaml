<Window x:Class="Noten.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Noten Settings" Height="430" Width="520" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" Content="Always On Top" IsChecked="{Binding AlwaysOnTop}" Margin="0,0,0,8"/>
        <CheckBox Grid.Row="1" Content="Minimize to tray on close" IsChecked="{Binding MinimizeToTrayOnClose}" Margin="0,0,0,8"/>
        <CheckBox Grid.Row="2" Content="Start with Windows" IsChecked="{Binding StartWithWindows}" Margin="0,0,0,8"/>
        <CheckBox Grid.Row="3" Content="Start minimized to tray" IsChecked="{Binding StartMinimizedToTray}" Margin="0,0,0,8"/>
        <CheckBox Grid.Row="4" Content="Confirm before exit" IsChecked="{Binding ConfirmBeforeExit}" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Theme" VerticalAlignment="Center" Width="80"/>
            <ComboBox SelectedIndex="{Binding ThemeIndex}" Width="180">
                <ComboBoxItem>System</ComboBoxItem>
                <ComboBoxItem>Light</ComboBoxItem>
                <ComboBoxItem>Dark</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Hotkey" VerticalAlignment="Center" Width="80"/>
            <TextBox Text="{Binding Hotkey}" Width="180"/>
            <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Foreground="#666" Text="(e.g. Ctrl+Space)"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Vertical" Margin="0,0,0,8">
            <TextBlock Text="Data path" FontWeight="SemiBold"/>
            <TextBox Text="{Binding DataPath}" IsReadOnly="True" Background="#F2F2F2"/>
        </StackPanel>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,12,8,0" IsDefault="True" Click="Save_Click" Content="Save"/>
            <Button Width="90" Margin="0,12,0,0" IsCancel="True" Content="Cancel"/>
        </StackPanel>
    </Grid>
</Window>
